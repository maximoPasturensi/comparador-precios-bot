# Proyecto_Precios

Virtual environment: `venv/` already exists in the project root.

Activation (macOS / Linux):

```bash
source venv/bin/activate
# or
. venv/bin/activate
```

Deactivate:

```bash
deactivate
```

Install dependencies:

```bash
pip install -r requirements.txt
```
